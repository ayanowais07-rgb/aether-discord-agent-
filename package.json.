{
  "name": "aether-autonomous-agent",
  "version": "4.0.0",
  "description": "Aether — Autonomous Browser Agent",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "postinstall": "node -e \"try{require('puppeteer')}catch(e){console.log('Installing puppeteer browser...')}\" && npx puppeteer browsers install chrome 2>/dev/null || true"
  },
  "dependencies": {
    "discord.js": "^14.15.3",
    "dotenv": "^16.4.5",
    "puppeteer": "^22.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
